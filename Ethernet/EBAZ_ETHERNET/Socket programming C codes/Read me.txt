The first file (Call the main page with IP address.cpp) is a C code. When you execute it, it will it will create a TCP socket and connect to 34.223.124.45 address (which is the ip address of nossl.com)
